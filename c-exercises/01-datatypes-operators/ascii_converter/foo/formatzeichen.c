#include <stdio.h>

/*Write a program that converts a asci caracter to the corresponding decimal
 * value.*/
#define ASCII_OFFSET 32

int main(void) {
  char uppercase_F = 'F';
  printf("The variable: %d is %c\n", uppercase_F, uppercase_F);
  return 0;
}
